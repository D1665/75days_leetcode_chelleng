class Solution {
    public void reverseString(char[] s) {
        int n=s.length;
        char temp='a';
        for(int i=0;i<n/2;i++){
            temp=s[i];
            s[i]=s[n-1-i];
            s[n-1-i]=temp;
        }
        
    }
}

//logic 
//swap the element till mid of the array
